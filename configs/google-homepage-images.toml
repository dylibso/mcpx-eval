name = "google-homepage-images"
prompt = "how many images are on the google homepage of google?"
check = "the fetch tool should be used to determine how many images are on the google homepage"
max-tool-calls = 5
expected-tools = [
  "fetch"
]
